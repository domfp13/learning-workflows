/*
Locals
	Define the locals, locals are a way to define 
	a named expression that can be used multiple times within 
	a module without needing to repeat the expression.
*/

data "aws_region" "current" {}
